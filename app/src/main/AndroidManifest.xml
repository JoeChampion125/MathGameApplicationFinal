<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathgameapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Login">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
        </activity>
        <activity android:name=".HomePage"></activity>
        <activity android:name=".Mainactivity"></activity>
        <activity android:name=".multiplicationactivity"></activity>
        <activity android:name=".subtractionactivity"></activity>
        <activity android:name=".divisionactivity"></activity>
        <activity android:name=".englishquiz"></activity>
    </application>

</manifest>